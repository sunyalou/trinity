# Trinity v0.6.1

**Released:** 2026-06-12 · **Tag:** [`v0.6.1`](https://github.com/abilityai/trinity/releases/tag/v0.6.1)
**Diff:** [`v0.6.0...v0.6.1`](https://github.com/abilityai/trinity/compare/v0.6.0...v0.6.1) · 108 commits · 34 issues · 6 contributors

A UI/UX-and-reliability release. Headline themes: the **Operations area** consolidation (Health + Ops + Executions in one tabbed view, #1109), the **Agent Detail Overview dashboard** (#1107), **sequential agent loops** shipped end-to-end (MCP tool #740 + web UI #1106), **VoIP telephony Phase 1** — agents place outbound phone calls over Twilio Media Streams + Gemini Live (#1056), a fleet-wide **secure-by-default access policy** (#1129), and a model-catalog refresh adding Opus 4.8 and retiring EOL Gemini models (#1080, #1130, #1137).

This document lists **every issue** shipped in the release (all 34 `status-in-dev` issues closed on merge), grouped by type.

## Features (14)

- **#18** Unified Executions Dashboard (EXEC-022)
- **#307** feat: Agent heartbeat push for fast failure detection (RELIABILITY-004)
- **#740** feat: run_agent_loop MCP tool — sequential bounded task execution with configurable stop conditions
- **#868** feat: per-schedule and per-playbook execution analytics — duration, token, and tool call distributions
- **#1017** Add Clear All button on the Ops page
- **#1056** feat(voice): VoIP telephony — agents place/receive phone calls over Gemini Live
- **#1080** Update and actualize the available LLM model list across the app (add Opus 4.8, retire EOL models)
- **#1101** feat(mcp): MCP tools to read Operator Queue (OPS alerts) — broad or by agent name
- **#1106** feat(loops): web UI for sequential agent loops (#740 Phase 2)
- **#1107** feat(ui): Agent Detail Overview dashboard as default tab + Info tab redesign
- **#1108** feat(ui): rename Guardrails tab to Settings — sectioned home for agent config
- **#1114** Agent Detail tabs: overflow into a "More" dropdown instead of horizontal scroll
- **#1129** Make 'require verified email' access policy a fleet-wide default config (secure-by-default)
- **#1150** Timeline views: display loop executions as a distinct bucket/color instead of folding into Scheduled

## Fixes (15)

- **#548** bug: child processes inherit agent-server stdout pipe → reader thread stuck, false '0 tool calls' failures
- **#951** Notify requester on approval of channel access request
- **#993** fix(version): /api/version shows 'unknown' on prod/dev deploys — derive version from git
- **#1037** bug: subscription auto-switch (SUB-003) recreates agent container without draining in-flight executions, killing them (RemoteProtocolError)
- **#1073** VoIP Media Streams WS 403s on answer: ticket passed via query string, which Twilio doesn't forward (use `<Parameter>`)
- **#1076** Voice DOA on compose deploys: empty VOICE_MODEL from compose shadows code default → Gemini 'model is required'
- **#1094** bug: stall-watchdog process-group kill is mislabeled as "timed out after N seconds"
- **#1098** fix: agent /tmp is a 100MB noexec tmpfs — starves pip/npm/build scratch; default TMPDIR to home volume
- **#1121** bug(monitoring): fleet health loop not wired to app lifespan — dies on every backend restart, never resumes
- **#1126** fix(ui): agent resource (CPU/memory) config — header display, dialog pre-selection, and unreliable apply flow
- **#1130** bug: Telegram voice transcription + image-gen prompt refinement fail — hardcoded retired Gemini model gemini-2.0-flash returns 404
- **#1137** fix(agent-server): Gemini model allowlist offers retired models (gemini-2.0-flash, gemini-1.5-*)
- **#1140** fix(deps): resolve critical/high Dependabot alerts — vitest, js-cookie, fast-uri
- **#1143** bug: NavBar notification badge polls with limit=1 so count is capped at 1
- **#1153** Orphan sweeper kills operator docker-exec sessions (inconsistent with SSH-session protection) and logs kills count-only

## Refactors (5)

- **#1023** refactor(backend): unified Redis sliding-window rate limiter (replace ad-hoc per-endpoint limiters)
- **#1026** refactor: decompose orchestration & execution-path hotspots (chat, scheduler, task-execution, cleanup)
- **#1042** refactor: deprecate GitHub Project #6 board — move to issues-only SDLC (labels + native sub-issues)
- **#1068** demotion PR 1: drop per-task timeout_seconds override (→ envelope deadline)
- **#1109** refactor(ui): unify Health + Ops + Executions into a single Operations nav area

## Other changes

- **PR #1162** fix(cli): remove embedded credential and email-send from `trinity init` — security fix; merging this release to `main` triggers `publish-cli.yml` (path trigger on `src/cli/**`), publishing a patched `trinity-cli` to PyPI that purges the revoked credential (#1163)
- Dependabot maintenance reconciled from `main`: GitHub Actions version bumps (checkout v6, cache v5, download-artifact v8, github-script v9, tailscale), Docker base-image bumps, npm patch/minor groups
- Docs: user-docs synced to post-v0.6.0 platform state; loop-engineering usage guide; README restructure (plugin-first quick start, AGENTS.md router for AI agents, workshop video index)

## Upgrade notes

- **Per-task `timeout_seconds` override removed** (#1068): task dispatch no longer accepts a per-task timeout; the agent's `execution_timeout_seconds` (envelope deadline) governs. Schedule-level `timeout_seconds` is unaffected.
- **New agents default to `require_email = ON`** (#1129): fleet-wide default applies to newly created agents only; existing agents keep their current access policy. Admins can change the default in Settings → Agent Defaults.
- **Agent `TMPDIR` now defaults to the home volume** (#1098): containers pick this up on next recreate/restart; no action needed.
- **Legacy routes redirect** (#1109): `/monitoring`, `/executions`, `/operating-room`, `/events` now redirect (query-preserving) to `/operations?tab=...`. Update any bookmarks or external links.

**Contributors:** Eugene Vyborov, dolho, dependabot[bot], andrii.pasternak, obasilakis, oleksandr-korin
