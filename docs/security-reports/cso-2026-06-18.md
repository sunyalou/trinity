# CSO Audit — `--diff` (branch `feature/1160-migration-runner-atomicity`)

**Date**: 2026-06-18 · **Mode**: daily (8/10 gate) · **Scope**: branch diff vs `dev` merge-base

## Scope
5 changed code files: `db/migrations.py`, `db/migration_lock.py` (new), `database.py`, `main.py`, `tests/unit/test_1160_migration_atomicity.py`. Change class: SQLite migration-runner hardening (cross-process flock + atomic table rebuilds + `/health` migration-completeness helper). No new endpoints, auth boundaries, credentials, or user-input paths.

## Findings
**0 findings at or above the daily 8/10 confidence gate.**

### Verified clean (with evidence)
- **A03 Injection — f-string SQL in `_atomic_rebuild`** (`migrations.py:100`): identifiers are interpolated (`f"DROP TABLE {table}"`), but both call sites pass **string literals** (`"agent_sharing"` @214, `"agent_skills"` @530); `create_new_sql`/`copy_sql` are hardcoded literals. No user input reaches the builder; SQLite can't bind identifiers anyway. Not exploitable.
- **Path handling — `migration_lock` lock file** (`migration_lock.py:34`): `lock_path = f"{db_path}.migrate.lock"` where `db_path` is the `DB_PATH` config constant (`database.py:172`), never request input. No path injection.
- **Secrets (P2)**: no secret-like patterns added in the diff; lock-failure log emits the `OSError` only, no credentials.
- **Dangerous sinks (P3/A03)**: no `subprocess`/`eval`/`exec`/`pickle`/`yaml.load`/`verify=False` in production code (the `multiprocessing` test workers are test-only).
- **A02 Crypto / A10 SSRF / P7 LLM-AI**: untouched by this diff.

### Below-gate observation (not a finding)
- **`/health` 503 now includes `first_pending` migration name** (`main.py`, unauthenticated endpoint). Confidence ~3/10. The migration names are already public (open-source `migrations.py`), exposed only during a transient migrations-incomplete 503 window, and reveal nothing exploitable. No action.

## STRIDE (changed component: migration runner)
- **Tampering**: rebuilds now atomic (explicit `BEGIN/COMMIT`) — a crash can't leave a half-rebuilt access-control table. Net **improvement**.
- **DoS**: `LOCK_EX` is unbounded but fail-open and bounded in practice by the holder's fast suite + kernel-release-on-death (excluded per DoS rule; documented in code).
- **Elevation/Spoofing/Repudiation/Info-disclosure**: unchanged.

## Trend
Prior reports are full-codebase scans (2026-01..03); this is a diff-scoped audit, not directly comparable. No regressions introduced.

## Verdict
**PASS** — the change is security-neutral-to-positive (closes an access-control data-loss window and serializes concurrent migration runs). No remediation required.
