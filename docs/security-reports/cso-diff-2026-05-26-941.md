# CSO --diff Report: PR #910 / Issue #941

**Date:** 2026-05-26
**Mode:** `/cso --diff` (sprint-scoped)
**Branch:** `feature/847-enterprise-sso-poc` (post-rebase, 10 commits ahead of `dev`)
**Audited commit:** `2068cee9` — feat(audit): enterprise audit log dashboard (#941) + remove SSO PoC

## Scope

This audit covers only the changes introduced in the current sprint
for the audit log dashboard feature. Pre-existing #910 Phase 0 commits
were audited when they originally merged.

In scope:

- `GET /api/audit-log/distinct/event-types` (new, admin-only)
- `GET /api/audit-log/distinct/actor-types` (new, admin-only)
- `db/audit.py` — `get_distinct_event_types` / `get_distinct_actor_types`
- `database.py` facade additions
- `src/frontend/src/views/enterprise/Audit.vue` (new, 297 lines)
- `src/frontend/src/stores/auditLog.js` (new, 175 lines)
- Submodule pointer bump (`3e90ddc → 1e916f6`)
- Removed: SSO mock surface (`SSO.vue`, Login.vue SSO buttons,
  submodule `backend/sso/`)

## Attack Surface (Sprint-Introduced)

| Surface | Count |
|---|---|
| Unauthenticated endpoints | 0 |
| Authenticated endpoints | 2 (both admin-only) |
| WebSocket channels | 0 |
| File upload points | 0 |
| External integrations | 0 |
| New env vars / secrets | 0 |
| New scheduled jobs | 0 |
| LLM / MCP tool surface | 0 |

## Findings

**No CRITICAL or HIGH findings.** Three LOW-severity stale-doc items
inherited from /review (already user-deferred as I3).

### Informational

| # | Severity | Category | Location | Issue |
|---|----------|----------|----------|-------|
| L1 | LOW | Doc hygiene | `docs/memory/feature-flows/enterprise-modules.md` (≈8 lines) | Documents deleted SSO code as current. Not exploitable. |
| L2 | LOW | Doc hygiene | `docs/memory/requirements.md:2425,2433` | Same — refs deleted `/api/enterprise/sso/*` endpoints. |
| L3 | INFO | Code hygiene | `src/backend/dependencies.py:212` | Docstring example uses `requires_entitlement("sso")`. Example is still valid usage; cosmetically stale. |

## Verification Matrix

| Concern | Method | Result |
|---|---|---|
| New endpoints admin-gated | Static check via `test_distinct_endpoints_admin_gated_and_before_catch_all` | ✅ Both have `Depends(require_admin)` |
| Router ordering (invariant #4) | Same test | ✅ `/distinct/*` declared before `/{event_id}` |
| SQL injection | Read `db/audit.py` distinct methods | ✅ No interpolation, no user input in query body |
| XSS in Audit.vue | `grep "v-html\|innerHTML"` | ✅ Zero matches |
| Filter input handling | Trace `axios.get(... params: filters)` | ✅ axios URL-encodes; template re-renders via `{{ }}` auto-escape |
| Information disclosure | Compare Audit.vue side-panel fields vs `AuditLogEntry` model | ✅ Same fields as existing `/api/audit-log/{event_id}` |
| Client-side route bypass | Manual trace of router guard | ⚠ Possible but harmless — backend admin gate is authoritative; bypass yields empty UI, no data leak |
| Orphaned SSO refs in executable code | `grep` across `src/` | ✅ All matches are docs / dead comments / test deny-assertions |
| Submodule SHA reachable | `git -C src/backend/enterprise log -1 --decorate` | ✅ Pushed to `origin/feature/941-audit-registration` |

## Variant Analysis

Pattern search across the codebase for the only "non-clean" item
(stale SSO references): 8 instances, all in docs / dead comments /
test deny-assertions. None create exploitable code paths.

## Trend vs Prior Reports

Compared against `cso-2026-04-13.md`:

- **Resolved**: 0 (sprint doesn't touch prior-finding files)
- **Persistent**: 0
- **New**: 0
- **Net direction**: Neutral

## Recommendations

1. **Defer L1/L2/L3** to a doc-cleanup follow-up after merge — they
   don't block the PR and the user already opted to defer during
   /review.
2. **Proceed to /sync-feature-flows + commit + push.** No security
   blockers identified.
3. **Future**: When `requires_entitlement("audit")` becomes
   meaningful (e.g., real SSO/SCIM lands), re-evaluate whether the
   existing `/api/audit-log/*` endpoints should be flipped to
   entitlement-gated — currently OSS by design (premise 3, /autoplan
   audit trail).

## Disclaimer

`/cso --diff` is an AI-assisted scan constrained to the sprint's
changes. For production deployment of enterprise-tier features
(audit-log dashboard included), a professional penetration test
of the OSS-Enterprise boundary is recommended before first paying
customer signs the contract.
